#pragma once

namespace Pinetime {
  namespace Identity {
    // Your handle. Shown on the NetRunner watch face and the Badge app, and used as the Bluetooth name.
    // Note: companion apps such as Gadgetbridge look for a name starting with "InfiniTime" when pairing a new watch.
    static constexpr const char* handle = "VOIDSEC";
  }
}
